ticket-prices:
  adult: 25.0
  student: 18.0
  child: 12.5

server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/multiplex
    username: jordanmruczynski
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  main:
    web-application-type: servlet
  flyway:
    baseline-on-migrate: true
  application:
    name: multiplex-backend-dev
